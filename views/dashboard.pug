
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Document
        link(href='/dashboard.css', rel='stylesheet')
        script(src="https://code.jquery.com/jquery-2.2.4.min.js", integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=",crossorigin="anonymous")
        link(href="https://fonts.googleapis.com/css?family=Roboto|PT+Sans", rel="stylesheet")
        script(src='dashboard.js', type='text/javascript')
        script(src='https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.6.0/slick.min.js', type='text/javascript' )
        script(src='https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.6.0/slick-theme.min.css', type='text/javascript')
        link(href='https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.6.0/slick-theme.css')
    body
        div(class='banner')

        div(class='content')
            div(class='user-details')
                div(class='user-head')
                    h1
                        div(class='user-img')
                        | Hello, #{user.username}!
                    h4
                        | Role : #{user.role}


                if(user.role === 'teacher')
                    a(href = '/assign-group')
                        img(src='blackboard.png')
                        | Create Class
                else
                    a
                        img(src='group.png')
                        | Chat Group
                a(type='button')
                    img(src='network.png')
                    | Connect
                a(type='button')
                    img(src='friends.png')
                    | Friends
                a(type='button')
                    img(src='settings.png')
                    | Settings
                a(href='/logout')
                    img(src='logout.png')
                    | Log Out

            div(class='team-scores')
                h1 Current Standings
                img(src='trophy.png')
                each val, index in scores
                    div(class=`score team`+(index+1), value=val)
        div(class='clearfix')
        
            

        div(class='assignments')
            h1 Current Assignments
            div(class='carousel-container')
                each question in questions
                        div(class='assignment-card')
                            | #{question.name}
                            //- if user.role == 'teacher'
                            //- button Assign to all students
            
